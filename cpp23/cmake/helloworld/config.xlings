-- https://github.com/d2learn/xlings

xname = "cpp23-cmake-helloworld"

xim = {
    cmake = "4.0.2",
    ninja = "1.12.1",
    gcc = "15",
}

d2x = {
    commands = {
        init = "cmake -S . -B build -G Ninja",
        build = "cmake --build build",
        run = "./build/helloworld",
        clean = "rm -r build",
    }
}